import { type IGestureEvent } from '@huangjs888/gesture';
import type { SwiperModel, ItemModel, ICallback } from '../core';
export default function rotate(this: SwiperModel<ItemModel | null>, event: IGestureEvent, { contextMenu }: ICallback): void;
